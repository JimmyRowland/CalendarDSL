package model.io;

import model.Scheduler;

// option
public class Writer {
    public void write(String path, Scheduler scheduler){
        // todo walk scheduler to format data to text
        // todo write file
    }
}
